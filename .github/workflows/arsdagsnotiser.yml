name: Årsdagsnotiser

on:
  schedule:
    # Actions-cron är i UTC, och körningar startar ofta sent. Fyra försök täcker
    # sommar- och vintertid med marginal; skriptet skickar tidigast 16 svensk tid
    # och minns per prenumeration vilket datum den senast fick något, så bara den
    # första körningen som lyckas gör något.
    - cron: "0 14 * * *"
    - cron: "30 15 * * *"
    - cron: "30 17 * * *"
    - cron: "30 19 * * *"
  workflow_dispatch:
    inputs:
      tvinga:
        description: "Kör även om klockan inte är 16 i Stockholm"
        type: boolean
        default: true
      torrkorning:
        description: "Visa vad som skulle skickas, utan att skicka"
        type: boolean
        default: true

concurrency:
  group: arsdagsnotiser
  cancel-in-progress: false

jobs:
  skicka:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm install web-push@3 firebase-admin@12 --no-save
      - run: node scripts/send-arsdagar.js
        env:
          VAPID_PRIVATE: ${{ secrets.VAPID_PRIVATE }}
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          TVINGA: ${{ inputs.tvinga && '1' || '' }}
          TORRKORNING: ${{ inputs.torrkorning && '1' || '' }}
